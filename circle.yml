general:
  artifacts:
    - "app/build/outputs/apk/app-release-unsigned.apk"
machine:
  java:
    version: openjdk7
  environment:
    ANDROID_HOME: /usr/local/android-sdk-linux
dependencies:
  override:
    - echo y | android update sdk --no-ui --filter "android-25,build-tools-25.0.2"
test:
  override:
    - echo "Nothing to do here"
deployment:
  master:
    branch: master
    commands:
      - ./gradlew assembleRelease
      - curl \
        -F "token=3260cbf8b275099a2596e23f6618ca159951cf11" \
        -F "file=app/build/outputs/apk/app-release-unsigned.apk" \
        -F "message=deployed" \
        https://deploygate.com/api/users/kazuyuka76/apps